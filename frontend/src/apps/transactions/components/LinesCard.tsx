// qqdelete — stub. LinesCard eliminated 2026-08-01. Real logic in useLineCard.ts + DataGrid.
// This file exists only to prevent Vite import errors from legacy detail pages.
// Delete when legacy pages are removed.
import React from 'react';
const LinesCard: React.FC<any> = () => <div>LinesCard removed — use /td/ route</div>;
export default LinesCard;
