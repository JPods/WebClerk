delete_NotNeeded based on the schedule

core/views/__init__.py 

Care must be taken if the table 
name is not a standard "s" ending


        app_label = TABLE_APP_MAP.get(table_name, 'core')
        if table_name == "addresses":
            model_name = "Location"
        else:
            model_name = table_name.rstrip('s').capitalize()

Form versus table references to tables
  "tablename-table" suffix for all table references displayed in a table or list
  "table" only in forms or flat display

CommandPromptExample for creating app folder structures
# Example: Create Django app folders with standard structure
for app in transactions orgs users products; do
  mkdir -p $app/{migrations,templates/$app,static/$app}
  touch $app/{__init__.py,admin.py,apps.py,models.py,tests.py,views.py,urls.py}
  touch $app/migrations/__init__.py
done