# =============================================================================
# webClerk3 — Django Backend Environment
# =============================================================================
# This file is .gitignored.  A committed template lives at:
#   readmes/env-setup.md
#
# Quick-start:
#   1. cp .env-example .env        (if starting fresh)
#   2. Set DB_MODE to remote / local / write-through
#   3. python manage.py runserver 0.0.0.0:8000
# =============================================================================

DEBUG=True
SECRET_KEY=1799934e7281a7a092b0136d173e328889e6cc678d38e997dd54bf901e853df1

# =============================================================================
# DATA SET IDENTIFICATION
# =============================================================================
# Helps identify which environment/database is in use.
# The React frontend reads a matching VITE_DATA_SET_ID in its own .env.
# Options: LOCAL, DEV, STAGING, PRODUCTION, or a custom identifier
DATA_SET_ID=DEV
DATA_SET_NAME=Development Server

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Toggle: Change this ONE line to switch databases.
# Options:
#   "remote"        → Team / production Postgres on the VPS
#   "local"         → Local Postgres (for offline / debugging)
#   "write-through" → Read local, save to remote, store response locally
DB_MODE=remote

# Remote Postgres (team collaboration)
# REMOTE_DATABASE_HOST=85.31.234.194
REMOTE_DATABASE_HOST=76.13.185.210
REMOTE_DATABASE_PORT=5432
REMOTE_DATABASE_NAME=commerce_expert
REMOTE_DATABASE_USER=postgres
REMOTE_DATABASE_PASS=wc_psql_server
# new server:
# ssh root@76.13.185.210
# password: webClerk@3.0

# Local Postgres (debugging)
LOCAL_DATABASE_HOST=localhost
LOCAL_DATABASE_PORT=5432
LOCAL_DATABASE_NAME=commerce_expert
# qaq MUST be change to your own local Postgres username.  If you don't have one, create a new Postgres role with login permissions and use that.
LOCAL_DATABASE_USER=williamjames
LOCAL_DATABASE_PASS=

# =============================================================================
# WRITE-THROUGH CONFIGURATION (DB_MODE=write-through)
# =============================================================================
# Timeout in seconds for forwarding saves to remote
WRITE_THROUGH_TIMEOUT=60

# =============================================================================
# EMAIL (iCloud SMTP)
# =============================================================================
EMAIL_HOST=smtp.mail.me.com
EMAIL_PORT=587
EMAIL_HOST_USER=antor.mee@icloud.com
EMAIL_HOST_PASSWORD=lvfu-fpxf-jluc-rzku

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================
SENTRY_DSN=

