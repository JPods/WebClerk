# Setting Model

This model manages application settings with purposes like view_edit, constants, db_defaults, etc.

Fields:

- name: Setting name
- purpose: Purpose (view_edit, constants, db_defaults, etc.)
- role: Associated role
- model_target: Target model name
- data: JSON data storage

The directory contains the full React implementation with pages, services, types, and utils.
