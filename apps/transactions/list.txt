Draft backlog below (grouped, prioritized). Copy into an issues board when ready.

Top (High Impact / Low‑Medium Effort)

Configurable field-level caching metrics (expose cache hit/miss for permission + aggregation endpoints).
Management command: rebuild all view_edit matrices from a template (seed roles).
Aggregation: add optional currency normalization & gross margin %
Line models: add lightweight denormalized numeric columns (price_extended_dec, cost_extended_dec) for faster aggregates.
Add include_fields / exclude_fields query params to line list endpoints for payload trimming.
Batch write validation endpoint (POST dry-run lines) returning per-line errors without persisting.
Security / Auth 7. Per-org scoping: ensure parent_ref_id queries are constrained to user.org_id. 8. Add optimistic concurrency token (etag / version) to BaseLineModel writes. 9. Rate limit: separate stricter scope for aggregate endpoint bursts. 10. Audit log model for denied edit attempts (role, field, timestamp).

Performance / Scaling 11. Switch aggregation to annotated DB sums where JSON extended is consistently structured (after denormalization). 12. Introduce Redis cache layer (optional) behind current in-process LRU (pluggable backend). 13. Async bulk keyword refresh using chunked DB queryset iteration (prefetch size tuning).

Developer Experience 14. Fabric script or Makefile for common tasks (lint, test subsets, migrations). 15. Pytest markers to isolate TX vs CORE vs PERMISSION suites. 16. Add type stubs (mypy) for JSON structures (TypedDict) to reduce runtime key mistakes.

Testing Enhancements 17. Time-travel test for TTL window rollover (freezegun or manual monkeypatch). 18. Property-based tests (hypothesis) for aggregation with random decimal distributions. 19. Snapshot tests for auth matrix endpoints (ensure schema stability).

API & Contract 20. OpenAPI: custom schema component for authorization matrix; link from each model’s endpoints. 21. Add HEAD /healthz and /readiness endpoints (DB + cache sanity). 22. Pagination metadata: add total distinct parent lines count.

Data Quality / Integrity 23. Validation hook: ensure probability only on proposal lines (not others) or move to subclass. 24. Add soft-delete flag to line JSON ‘item’ or top-level? (Consistent deletion semantics). 25. Background task to normalize legacy JSON fields missing new keys (migrations alternative).

Observability 26. Structured logging (JSON) + correlation id middleware. 27. Metrics export (Prometheus) for request latency, cache hits, aggregate runtimes. 28. Slow query logging threshold config.

Front-End Support 29. “Capabilities bootstrap” endpoint: returns batch matrices + enum choices + throttle limits in one call. 30. Field diff endpoint: compare two versions of a line (before/after edit).

Maintenance / Cleanup 31. Remove unused legacy endpoints or consolidate universal API docs with new tx endpoints. 32. Enforce consistent naming: parent_ref_id vs parent_id across apps (add lint check). 33. Central constants module for model code strings (avoid typos in tests/views).

Longer-Term / Strategic 34. Event sourcing or change journal for lines (append-only log for analytics). 35. Materialized aggregate table updated via signals for large parent sets. 36. Pluggable rules engine (DSL) replacing static view_edit JSON (future-proofing complex conditions).

Nice-to-Have / Low Effort 37. Django admin actions: bulk regenerate matrices, clear caches. 38. CLI: python manage.py tx_stats (counts per line model, recent activity). 39. README badges (tests status, coverage later).

Housekeeping / Risks 40. Pin versions in requirements.txt and add hash checking. 41. Add security scan (bandit) & dependency audit workflow. 42. Document disaster recovery (DB restore + cache warm plan).

Quick Wins (suggest doing next)

#4 Denormalized numeric columns
#11 DB aggregate shift (after #4)
#17 TTL rollover test
#29 Capabilities bootstrap
#37 Admin cache clear action
Let me know which 3–5 you want queued first and I’ll prep the changes.