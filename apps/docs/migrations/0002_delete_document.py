from django.db import migrations


class Migration(migrations.Migration):
	# Converted to a follow-up no-op migration after squash cleanup.
	# Retained filename to satisfy any historical references, but chained to the real 0002.
	dependencies = [
		('docs', '0002_linkage_is_active'),
	]

	operations = []
