Build 2 and 3 as out lined and add a training tab in product for tracting
  the flow of inventory on a specific item.  So the db.list will show
  ida-bb200 (example) with its starting inventory of on_hand, on_p (proposal),
  on_so (order), on_i, (invoice), on_po (purchase), on_wo (work_order), on_r
  (receipt - check pydantic schema), on_adj (adjustment). The user then adds a 
  proposal for 15 units and saves it. The db.list changes to show the           
  proposal_line and the pending records. The user then creases an order from    
  the proposal for 9 of the units. Saving updates the proposal line record,     
  and adds the order_line record and pending. Then the user create an purchase  
  from the order for 14 and an invoice for 4. Then the user makes a receipt     
  from the purchase for 11.  The db.list show all the lines and pendings and    
  changes in the item record at each stage. Am I making sense? I want to teach  
  how to think about inventory flows and the importance of pending. 

  This db.list can also show how that line would be converted into gl amounts and accounts -- and where there is no gl impact


  We can have them add a partial payment, a discount, and dismisal of small 
   amount to who those gl behaviors                                           
─────────────────────────────────────

open -a "Chrome Debug"                                                        
   
  Or from the command line with flags:                                          
                                                                  
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \--remote-debugging-port=9222\user-data-dir="$HOME/.chrome-debug-profile"

⏺ React build passed clean. Here are your open items for next session:               
                                                                                     
  1. Test TouchBar in browser — refresh localhost:5173, open contact/action detail,  
  verify Call/Email/Text buttons appear                           
  2. Test Touch save — click a button, fill the form, confirm record saves to        
  database                                                                           
  3. Touch report backend — Call Summary, Email Draft, Visit Report need server-side 
  implementation                                                                     
  4. Verify org.type in databrowser for all org models            
  5. Clean up stacked VS Code terminals from my build attempts                       
                                                                Your todo list:                                                               
                                                                
  1. bash scripts/deploy-codemap.sh — push to webclerk.com/codemap/             
  2. Convert thumbnails from JPG to PNG (to match the standard we set)
  3. Purchase codemap.guru domain                                               
  4. Expand codemap.json node coverage (many flowcharts still at 0 mapped nodes)
  5. Teach Alice the image library resolution pipeline                          
                                                                
  Good session. Sleep well.         