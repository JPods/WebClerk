  1. metadata: add audit_trail, explanation, import_data with defaults. Remove  
  undefined, images.                                              
  2. refs: add source with default. Remove categories, depends_on, parents,     
  related_ids.                                                                  
  3. prefs: add search, and where missing add pinned, tags, userdefined. For
  company-profile, move its 10 domain keys to .config before removing from      
  prefs.    



We should have an explation for each item in .metadata, .refs, .prefs, and .config

Let's go through each json
.metadata items
What is the reason to add audit_trail and explanation?

I like the idea of import_data but it needs to be managed with {"dt_imported", "dt_delete_on", "status", } or similar. this will provide the ability to break a bundle import into manageable by record actions. But if not cleared, it will become the never emptied trashcan.

replace "undefined" with "userdefined" 

the reason for metadata.images.tn, base, hires, logo is to provide a path to these image. They are only needed in some models like contacts, orgs, item, document

My concern is if we dont provide a uniform framework, users will get creative and scatter these whereever.



P   